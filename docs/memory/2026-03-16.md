## 2026-03-16

- Pivoted `eng/web-app` and `eng/mobile-app` away from the neo-brutalist visual language toward a gentler minimalist system inspired by Skylight.
- Added `eng/web-app/src/lib/designSystem.ts` with warm cream, sage, muted gold, soft-charcoal tokens plus rounded surfaces, soft shadows, pill buttons, and lifestyle-photo placeholders.
- Restyled the main web entry and family-facing flows: `src/app/layout.tsx`, `src/app/globals.css`, auth (`login` and `signup`), sponsor booking, sponsor dashboard, and patient dashboard.
- Restyled the main mobile family-facing flows: theme tokens, `ActionCard`, auth, recipient dashboard, sponsor dashboard, sponsor booking, and payment stub/live wrapper.
- Applied the same gentle-minimalist system to the active doctor flows: web `DoctorDashboard`, mobile `DoctorDashboard`, `SoapNoteScreen`, and `PrescriptionUploadScreen`.
- Reworked the web doctor booking page at `eng/web-app/src/app/doctor/book/page.tsx` to match the provider workspace styling, use a calendar-first appointment flow (`date` + `time` inputs, quick date chips, duration selector), resolve the doctor from `/api/v1/auth/me`, and offer an `Add to calendar` link after successful booking.
- Moved the web doctor dashboard `Practice settings` card into a dedicated `/doctor/profile` page. The dashboard `Edit profile` action now opens that page, and the new profile screen includes editable doctor basics, credentials, scheduling preferences, and the moved fees/earnings controls.
- Reordered the web doctor dashboard so `Today’s flow` is now the second major card directly below the `Provider workspace` hero, with care coordination and insights following underneath.
- Updated the web patient portal to use full-width page shells with responsive `clamp(...)` side gutters instead of the shared `pageShell` max-width cap, so the patient dashboard now stretches across the webpage like the doctor portal.
- Replaced the web root redirect with a real full-width landing page at `eng/web-app/src/app/page.tsx`, removed `Sponsor` from the visible auth role selectors, reordered the entry roles to `Patient`, `Doctor`, `Pharmacist`, and made the login/signup shells plus global top bar use full-width responsive gutters.
- Cleaned up remaining placeholder/design-reference copy in the web auth and entry flows, replacing labels like `Gentle Minimalist`, `Lifestyle photo placeholder`, and palette/style chips with service-specific language such as `Dhanvanthri care portal`, `Care at home`, `Care-first`, and `Trusted guidance`.
- Fixed appointment-booking auth mismatch: backend `POST /api/v1/appointments` now allows `RECIPIENT` in addition to `DOCTOR` and `SPONSOR`, and the web booking forms now fail fast on missing JWTs and show clearer 401/403 messages instead of a raw `HTTP 401`.
- Wired booking into the web patient portal: added `GET /api/v1/auth/doctors`, added `/patient/book` with a patient-friendly booking flow that auto-fills the logged-in patient and loads doctors from the backend directory, and exposed `Book appointment` actions in the patient dashboard hero and quick actions.
- Fixed a Java compile issue in `AuthController#doctors()` by replacing `Map.of(...)` with an explicit `LinkedHashMap<String, Object>` so Maven no longer infers an incompatible intersection type for the doctor-directory response.
- Web verification: `cd eng/web-app && npm run type-check` passed after fixing one duplicated inline `color` property in `src/app/login/LoginForm.tsx`.
- Mobile verification is still incomplete in this workspace because `eng/mobile-app/node_modules/.bin/tsc` is missing, so `npm run type-check` cannot run until mobile dependencies are installed.
