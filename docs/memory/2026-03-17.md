## 2026-03-17

- Added a real web admin portal at `eng/web-app/src/app/admin/*` with a Skylight-inspired warm-neutral layout, persistent sidebar navigation, top header, and rounded soft-shadow surfaces.
- Introduced reusable admin UI building blocks in `eng/web-app/src/components/admin/AdminDashboardLayout.tsx` and `eng/web-app/src/components/admin/AdminSectionPage.tsx`.
- Added working admin section routes for `Overview`, `Practitioners`, `Patients`, `Pharmacists`, `Video Ops`, and `Audit Logs` so the sidebar navigation resolves instead of pointing to dead links.
- Enabled Tailwind CSS and `lucide-react` in `eng/web-app` by adding `tailwindcss`, `@tailwindcss/postcss`, `postcss`, `autoprefixer`, `lucide-react`, `postcss.config.js`, and `@import "tailwindcss";` in `src/app/globals.css`.
- Verified the admin portal with `cd eng/web-app && npm run build` and a follow-up `npm run type-check`; the first type-check had initially failed only because this repo includes `.next/types/**/*.ts` and needed a fresh build to regenerate route types.
- Added a backend operations summary endpoint at `eng/src/main/java/com/dhanvanthri/ops/controller/OperationsController.java` backed by `OperationsSummaryService`, aggregating live data from users, appointments, medications, prescription queue state, and prescription audit logs for the web admin portal.
- Reworked the web admin pages to fetch live server-side data from `/api/v1/ops/summary` via `eng/web-app/src/lib/adminServer.ts`, and restricted `/admin` access to signed-in `DOCTOR` or `PHARMACIST` sessions until a dedicated `ADMIN` role exists.
- Fixed a latent SQL bug in prescription queue queries by replacing `SELECT days_remaining FROM medications` with the computed expression `(total_quantity / NULLIF(daily_dosage, 0)) AS days_remaining` in both `PrescriptionController` and the new ops summary service.
