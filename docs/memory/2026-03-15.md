## 2026-03-15

- Fixed `dhanvanthri-web` App Runner deployment in `ap-south-1`.
- Root cause: Next.js standalone `server.js` reads `process.env.HOSTNAME`; App Runner runtime injected its own internal hostname, so the app bound to that host instead of `0.0.0.0`. Failed service logs showed `Local: http://ip-10-0-188-219...:3000`, which matched the TCP health-check failure.
- Fix: added `eng/web-app/apprunner-entrypoint.cjs` to force `HOSTNAME=0.0.0.0` before requiring `server.js`, updated `eng/Dockerfile.web` and `eng/web-app/Dockerfile.prod` to use it, and switched the web App Runner health check to `HTTP /api/health`.
- Patched `ops/aws/05-setup-apprunner.sh` so reruns delete a `CREATE_FAILED` web service and recreate it, instead of issuing a no-op update.
- Rebuilt and pushed `246599827879.dkr.ecr.ap-south-1.amazonaws.com/dhanvanthri/web:latest`, deleted failed service `6555399c8e8d4a5da16ed55c34a2a0a9`, and recreated `dhanvanthri-web` as service `4be4632384fd4d42ac1d7d83bd1e1c3d`.
- Verified final web URL `https://jxbxjk37jm.ap-south-1.awsapprunner.com` is `RUNNING` and `GET /api/health` returns `{"status":"ok"}`.
- Fixed backend CORS for the App Runner frontend by making `eng/src/main/java/com/dhanvanthri/shared/config/SecurityConfig.java` read `app.cors.allowed-origin-patterns`, defaulting to localhost origins plus `https://*.ap-south-1.awsapprunner.com`.
- Updated `ops/aws/05-setup-apprunner.sh` to set `APP_CORS_ALLOWED_ORIGIN_PATTERNS` for the API service, rebuilt/pushed the API image, and triggered a fresh App Runner deployment.
- Verified the previously failing preflight now returns `HTTP 200` with `Access-Control-Allow-Origin: https://jxbxjk37jm.ap-south-1.awsapprunner.com`.
- Verified `POST https://jxbxjk37jm.ap-south-1.awsapprunner.com/api/v1/auth/register` no longer fails with CORS; it returned `201 Created` once and then `409 Email already registered` on repeat for test user `codex-test-20260316@example.com`.
